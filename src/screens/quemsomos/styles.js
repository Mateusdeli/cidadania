import styled from 'styled-components'

export const Wrapper = styled.div`
    position: relative;
    width: ${({ theme }) => theme.root.quem_somos.width}px;
    height: ${({ theme }) => theme.root.quem_somos.height}px;
`;
